/**
 * this is a simple function for displaying the added two number
 */
function testingTS(number1: number, number2: number): string {
  console.log("=================================");
  console.log(number1 + number2);
  console.log("=================================");
  return "return is String";
}

testingTS(1, 2);
